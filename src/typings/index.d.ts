namespace models {
  // Example usage of this type directly in other files: models.example.ExampleType
  namespace example {
    interface ExampleType {
      exampleText: string;
      exampleVal: number;
    }
  }
}